{
  "name": "strava-webhook",
  "version": "1.0.0",
  "description": "strava webhook",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"sudo node index.js\" \"lt --port 8000 --subdomain strava-webhook\"",
    "serve": "lt --port 8000 --subdomain strava-webhook"
  },
  "author": "Benoit GRASSET",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "concurrently": "^8.2.1",
    "cross-fetch": "^4.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "localtunnel": "^2.0.2",
    "ngrok": "^5.0.0-beta.2"
  }
}
